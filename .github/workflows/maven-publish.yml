name: Digio Teste Maven Package

on:
  release:
    types: [push, pull_request]
  push:
    branches:    
      - master     

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      - 
        uses: actions/checkout@v2
      - 
        name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
          server-id: github # Value of the distributionManagement/repository/id field of the pom.xml
          settings-path: ${{ github.workspace }} # location for the settings.xml file

      - 
        name: Build with Maven
        run: mvn -B package --file pom.xml

      - 
        name: Publish to DockerHub
        run: |
          ls target/
          mvn fabric8:build fabric8:push
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - 
        name: Deploy Kubernetes
        run: |
          #echo > '${{ secrets.KEY_PAIR }}' virg.pem
          ls
          cat ${{ secrets.KEY_PAIR }}
          #ssh -i "virg.pem" ubuntu@ec2-52-90-139-109.compute-1.amazonaws.com
          #microk8s kubectl apply -f kubernetes.yml 

